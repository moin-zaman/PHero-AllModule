18_1_New

/**
 * VARIABLE ==> container
*/

// number
var sunglassPrice = 80;

// string
var subject = "EEE";

// boolean
var passed = true;

/**
 * NAMING CONVENTION
 * 1. single word
 * 2. no quote
 * 3. no gap or dash
 * 4. can not start with number
 * 5. can not be a keyword
 * 6. prefer camelCase
*/

var lastExamResult = 89;

/**
 * OPERATION
 * 1. +, -, *, /, %
 * 2. +=, -=, *=, /=
 * 3. isNaN 
 * 4. null
 * 5. Number 
*/


/**
 * COMPARISON
 * 1. bigger: > 
 * 2. less: <
 * 3. equal: ==
 * 4. greater than or equal: >= 
 * 5. less than or equal: <=
 * 6. not equal: !=
 * 
 * -------------------
 * 7. and: &&
 * 8. or: ||
*/


18_2 -->

var isTrue = true;

if(isTrue)
{
    console.log("Hello");  //out: hello
}

var isTrue = false;

if(isTrue)
{
    console.log("Hello"); //out: no output
}

var isTrue = false;

if(isTrue == false) //  it will return true;
{
    console.log("Hello"); //out: Hello
}


18_9 -->

let inputValue = null;
console.log(inputValue);

if(!inputValue)
{
    console.log("Hello");
} output: Hello 

Falsy values in JavaScript (that make !value true):
false
0
'' (empty string)
null
undefined
NaN

*  var leader = false
if( !leader) --> Is leader == false ---> result is "true"
that means he is not a leader ;

if( leader) --> Is leader == true ---> result is "false"

** !!true = true
!!false = false

**************
let userID = 10;
console.log(!!userID)
console.log(!userID)
console.log(userID)

***************



18_5_3 -->

Array is a collection of variable of same Data Type

let arr = new Arr(3);
arr[0] = 1;
arr[1] = 2;
arr[2] = 3;

let a  = [1, "Moin", 2.50];



18_5_4 -->

we can set and modify and push the const array's values.

const arr = [];
arr[0] = 2;
arr[1] = 3;
arr.push(10, 20);
console.log(arr);

arr.pop();

arr.unshift(90);
arr.shift();


18_5_5 -->

let arr = [1, 4, 6, 8, 9];

let arr1 = arr.slice(0,2);
console.log(arr1); 

// slice() যেইসব element বাদ দিয়েছে সেগুলা বাদে remaining element দিয়ে নতুন আরেকটা array return করে . original array এর কোনো পরিবর্তন করে নাহ ।


let arr1 = arr.sunglassPrice(0,2);
console.log(arr1); 

splice() যেইসব element বাদ দিয়েছে সেগুলা সেগুলোর নতুন একটা array return করে । আর original array এর বাদ দেওয়া element বাদ দিয়ে modify করে ।

In Js string is Immutable that means we can not modify strings character using array index.
